server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/urban-events?currentSchema=metricas
    username: pguser
    password: localpass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    bootstrap-servers: localhost:9092

kafka:
  bootstrap-servers: localhost:9092
