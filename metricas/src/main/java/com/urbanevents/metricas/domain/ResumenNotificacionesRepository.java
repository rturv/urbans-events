package com.urbanevents.metricas.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ResumenNotificacionesRepository extends JpaRepository<ResumenNotificaciones, Long> {
}
